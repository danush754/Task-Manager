version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/db/queries"
    gen:
      go:
      
        emit_json_tags: true
        package: "db"
        out: "./internal/db"
        sql_package: "pgx/v5"

